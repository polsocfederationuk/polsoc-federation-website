{#
  Site footer — identical on all eight indexable pages in each language
  (verified by normalised comparison; only 404.html differs, and only in its
  link form). Contact-page-specific content is deliberately NOT pulled in here.

  Localised: organisation name, blurb, charity line, both column headings and
  the copyright sentence (all from ui[locale].footer).
  Invariant: the e-mail address and the three social URLs (from site.json).

  The brand <img> carries alt="" on purpose — it is decorative, because the
  organisation name sits directly beside it as text.
#}
<footer class="site-footer">
  <div class="container">
    <div class="footer-grid">
      <div class="footer-brand">
        <img src="{{ site.logo }}" alt="">
        <div>
          <strong>{{ t.footer.orgName }}</strong>
          <p>{{ t.footer.blurb }}</p>
          <p class="charity-no">{{ t.footer.charityLine }}</p>
        </div>
      </div>
      <div>
        <h4>{{ t.footer.exploreHeading }}</h4>
        <ul>
{%- for item in nav.footerExplore %}
          <li><a href="{{ item.file | navHref(locale, linkMode) }}">{{ t.nav[item.key] }}</a></li>
{%- endfor %}
        </ul>
      </div>
      <div>
        <h4>{{ t.footer.connectHeading }}</h4>
        <ul>
          <li><a href="mailto:{{ site.email }}">{{ site.email }}</a></li>
          <li><a href="{{ site.social.instagram }}" target="_blank" rel="noopener">Instagram</a></li>
          <li><a href="{{ site.social.linkedin }}" target="_blank" rel="noopener">LinkedIn</a></li>
          <li><a href="{{ site.social.facebook }}" target="_blank" rel="noopener">Facebook</a></li>
        </ul>
      </div>
    </div>
    <div class="footer-bottom">
      <span>{{ t.footer.copyright | safe }}</span>
      {#-
        Staff login (Phase 17D.1). The way in to the content manager for
        committee officers, sitting with the secondary footer text rather than
        being advertised anywhere prominent.

        Not a secret: the address is public and the security is invite-only
        accounts, verified roles and a server-side role check, not an
        unguessable URL. `rel="nofollow"` and the page's own noindex keep it out
        of search results, which is tidiness rather than access control.
      -#}
      {#-
        Root-relative, and the same address in both languages: there is one
        login page, not an English one and a Polish one. The widget on it
        follows the browser, and it resolves identically from / and from /pl/.
      -#}
      <span><a class="footer-staff-login" href="/staff-login/" rel="nofollow">{{ t.footer.staffLogin }}</a></span>
      <span>polsocfederation.pl</span>
      {#-
        NETLIFY ATTRIBUTION (open-source release). Netlify's open-source
        policy requires "a link to our service on your main page, or all
        internal pages". This partial is shared, so putting it here satisfies
        that in its strongest form: every page, in both languages.

        DO NOT DELETE THIS LINK. It is a condition of the Netlify Open Source
        Plan the charity's hosting credits depend on, not decoration. Its
        presence is asserted by scripts/compare-chrome.js,
        scripts/compare-404.js, scripts/compare-homepage.js,
        scripts/compare-events-listing.js, scripts/compare-business-forum.js
        and scripts/validate.js, so it cannot be dropped silently.

        Wording is Netlify's own suggested sentence in English and its
        translation in Polish (ui[locale].footer.poweredBy). target and rel
        match the social links in the column above.
      -#}
      <span><a href="https://www.netlify.com" target="_blank" rel="noopener">{{ t.footer.poweredBy }}</a></span>
    </div>
  </div>
</footer>
